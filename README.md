# VotersApp - Online Voting System

A web-based voting application with user authentication, candidate management, and real-time election results dashboard. Built with Flask and CSV-based data persistence.

## Installation

```bash
pip install flask pandas
python app.py
```

Access at `http://localhost:80`
